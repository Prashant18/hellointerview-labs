name: broker

services:
  dynamodb-local:
    image: amazon/dynamodb-local:2.5.2
    container_name: broker-dynamodb
    command: ["-jar", "DynamoDBLocal.jar", "-inMemory", "-sharedDb"]
    ports:
      - "${DDB_PORT:-18022}:8000"

  mock-exchange:
    build:
      context: ./mock-exchange
    image: broker-mock-exchange:latest
    container_name: broker-mock-exchange
    ports:
      - "${EXCHANGE_PORT:-18023}:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8000/health',timeout=1).status==200 else 1)"]
      interval: 2s
      timeout: 2s
      retries: 30
      start_period: 5s

  api:
    build:
      context: ./api
    image: broker-api:latest
    deploy:
      replicas: 3
    environment:
      - DDB_ENDPOINT=http://dynamodb-local:8000
      - AWS_DEFAULT_REGION=us-east-1
      - AWS_ACCESS_KEY_ID=local
      - AWS_SECRET_ACCESS_KEY=local
      - EXCHANGE_URL=http://mock-exchange:8000
    depends_on:
      dynamodb-local:
        condition: service_started
      mock-exchange:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8000/health',timeout=1).status==200 else 1)"]
      interval: 2s
      timeout: 2s
      retries: 30
      start_period: 5s

  caddy:
    image: caddy:2.8-alpine
    container_name: broker-caddy
    ports:
      - "${API_PORT:-18021}:8000"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
    depends_on:
      api:
        condition: service_healthy

  prometheus:
    image: prom/prometheus:v2.54.1
    container_name: broker-prometheus
    ports:
      - "${PROM_PORT:-19093}:9090"
    volumes:
      - ../shared/observability/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --web.listen-address=:9090
      - --storage.tsdb.retention.time=1h
    depends_on:
      api:
        condition: service_healthy

  grafana:
    image: grafana/grafana:11.2.2
    container_name: broker-grafana
    ports:
      - "${GRAFANA_PORT:-13003}:3000"
    environment:
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
      - GF_AUTH_DISABLE_LOGIN_FORM=true
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - ../shared/observability/grafana/provisioning:/etc/grafana/provisioning:ro
    depends_on:
      - prometheus

  k6:
    image: grafana/k6:0.53.0
    container_name: broker-k6
    profiles: ["test"]
    environment:
      - API_URL=http://caddy:8000
      - EXCHANGE_URL=http://mock-exchange:8000
    volumes:
      - ./k6:/scripts:ro
    entrypoint: ["k6"]
